# labdev-runtime

Local coding runtime powered by Aider + Ollama.

## Installation

1. Clone this repository:
   ```sh
   git clone https://github.com/your-repo/labdev-runtime.git
   cd labdev-runtime
   ```

2. Install dependencies:
   ```sh
   pip install -r requirements.txt
   ```

3. Set up the project:
   ```sh
   python src/labdev/cli.py init
   ```

## Usage

### Status Command

To show repository information:
